import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js';
import {EYE_HEIGHT,PLAYER_HEIGHT,PLAYER_RADIUS,GRAVITY,JUMP_SPEED,HEIGHT} from './config.js';
const solid=id=>id!=='air'&&id!=='water'&&id!=='leaves'&&id!=='glass';
export function collides(world,x,feet,z){for(let dx=-PLAYER_RADIUS;dx<=PLAYER_RADIUS;dx+=PLAYER_RADIUS)for(let dz=-PLAYER_RADIUS;dz<=PLAYER_RADIUS;dz+=PLAYER_RADIUS)for(let y=Math.floor(feet);y<=Math.floor(feet+PLAYER_HEIGHT);y++)if(solid(world.get(Math.floor(x+dx),y,Math.floor(z+dz))))return true;return false}
export function movePlayer(world,p,velocity,dt,keys){const dir=new THREE.Vector3((keys.has('KeyD')?1:0)-(keys.has('KeyA')?1:0),0,(keys.has('KeyS')?1:0)-(keys.has('KeyW')?1:0));if(dir.lengthSq())dir.normalize().applyAxisAngle(new THREE.Vector3(0,1,0),p.yaw);const speed=keys.has('ShiftLeft')?7:4.5;const dx=dir.x*speed*dt,dz=dir.z*speed*dt;if(!collides(world,p.x+dx,p.feetY,p.z))p.x+=dx;if(!collides(world,p.x,p.feetY,p.z+dz))p.z+=dz;const grounded=collides(world,p.x,p.feetY-.06,p.z);if(keys.has('Space')&&grounded&&!p.jumpLatch){velocity.y=JUMP_SPEED;p.jumpLatch=true}else if(!keys.has('Space'))p.jumpLatch=false;velocity.y-=GRAVITY*dt;let ny=p.feetY+velocity.y*dt;if(collides(world,p.x,ny,p.z)){if(velocity.y<0){while(collides(world,p.x,ny,p.z)&&ny<p.feetY)ny+=.02;velocity.y=0}else{velocity.y=0;ny=p.feetY}}p.feetY=Math.max(1,Math.min(HEIGHT-2,ny));return grounded}
export function syncCamera(camera,p){camera.position.set(p.x,p.feetY+EYE_HEIGHT,p.z);camera.rotation.set(p.pitch,p.yaw,0,'YXZ')}
